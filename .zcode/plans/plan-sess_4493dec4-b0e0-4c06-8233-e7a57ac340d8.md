# Stage 8F — General Settings Workspace

## Что
Превратить `/settings/general/` (`views/general_settings.hbs`) из legacy-набора Bootstrap panels в спокойное, структурированное рабочее пространство из 6 смысловых областей. Scoped под `.general-settings-page`. UI-этап: НЕ менять бизнес-логику, маршруты, authorization, модели, `public/js/settings_general.js`, locales, deps.

**Ветка** `inf/general-settings-workspace-redesign` off `origin/master` (`e3811a4`, PR #159 Stage 8E смержен). Scratch вне репо; без `.gitignore`; скриншоты → `/tmp/stage8f-general-settings/`.

## Факты установлены (проверено агентами + прямым чтением)
- **Authorization (НЕ трогать):** `router.all(/.*/, ensure_user_is_admin)` (`lib/route/settings.js:34`) — admin-only на весь `/settings` router.
- **`settings_general.js` (НЕ трогать):** делает ОДНУ вещь — `.leavetype-remove-btn` click → `window.confirm(data-confirm-message)` → append `value` к `#delete_leavetype_form` action → submit. `data-tom-*` — это **Selenium-хуки**, не JS-контракты. Color picker/dropdown/modal — чистый Bootstrap 3 `data-toggle`.
- **Route** `GET /settings/general/` (`settings.js:37-63`) рендерит `general_settings` с контекстом `{title, company (with_leave_types), schedule, countries, timezones_available, carryOverOptions, yearCurrent, yearPrev, leave_types}`.
- **Root `<main id="main-content" tabindex="-1">` БЕЗ класса.** h1 = `{{t "generalSettings.title"}}`, subtitle `.lead` = `{{t "generalSettings.subtitle"}}`. Нет `.page-heading`.
- **3 legacy `.panel`-блока:** company+backup+schedule+carryover (`.panel-default`), leave-types (`.panel-default.leave-types-panel`), bank-holidays (`.panel-default`), danger (`.panel-danger`).
- **Все защищённые контракты** задокументированы дословно (agent 1): `#company_edit_form` (action/CSRF/name required/country/date_format/timezone/carry_over/share_all_absences/is_team_view_hidden/.single-click), `#company_schedule_form` (action/CSRF/company_wide=1/schedule_widget/.single-click), `#calculate_carry_over_form` (action/CSRF), `#delete_leavetype_form`+`#leave_type_edit_form` (actions/CSRF), все leave-type inputs (first_record/name__/color__/use_allowance__/auto_approve__/limit__/deduction_unit__/minimum_consecutive_days__ + data-tom-* hooks + aria-labels + .leavetype-remove-btn/value/data-confirm-message), `#add_new_leave_type_btn`+`data-target`, backup `/settings/company/backup/`, bank-holidays `/settings/bankholidays/`, danger `data-target="#remove_company_modal"`, оба modal-партиала, `tst-no-bank-holidays` test-hook.
- **`settings_accessibility_contract.js`** блокирует 7 aria-label regexes на leave-type controls — сохранить verbatim.
- **Существующие global `.leave-types-*` правила** (main.scss:3015-3244 + color-picker-menu 3049-3064 + color classes 3273-3306 + dark 4651-4663) — НЕ редактировать; scoped-блок поверх.
- **SCSS-паттерны 8A–8E** (5341-6660) — canonical skeleton. Stage 8C (5799-6044) — лучший эталон.
- **Глобальные блоки НЕ трогать:** `:root` (65-79), global dark tokens (81+), huge global dark override (4465+), global `.mobile-card-table` (2275+), global `.panel` (938-976).

## 1. Разметка — `views/general_settings.hbs`
- **Scoped root:** `<main id="main-content" class="general-settings-page" tabindex="-1">`. Сохранить skip-link, `{{> show_flash_messages }}`, `{{> header/footer}}`.
- **`.page-heading`:** единственный h1 `{{t "generalSettings.title"}}` + `.lead` `{{t "generalSettings.subtitle"}}`.
- **A. Company profile surface** (`<section class="gs-company-surface surface">`): `#company_edit_form` (дословно: action/method/CSRF/all fields/required/.single-click). Без `.form-horizontal`/`.panel`. Двухколоночная grid внутри surface (company form | backup). **B. Schedule** — отдельная `<section class="gs-schedule-surface surface">`: `#company_schedule_form` (дословно: action/CSRF/company_wide=1/schedule_widget/.single-click). **C. Allowance carry-over** — `<section class="gs-carryover-surface surface">`: `#calculate_carry_over_form` (дословно) + пояснения; визуально отличить от обычного Save.
- **E. Related settings** — `<section class="gs-related-surface surface">`: backup `/settings/company/backup/` + bank-holidays `/settings/bankholidays/` (`tst-no-bank-holidays` сохранить).
- **D. Leave types editor** — `<section class="gs-leavetypes-surface surface">`: `#delete_leavetype_form`+`#leave_type_edit_form` (дословно), header, empty state, per-type rows (дословно: все inputs/data-tom-*/aria-labels/color-picker/.leavetype-remove-btn), footer (Add `#add_new_leave_type_btn`+`data-target` + Save `.single-click`). Сохранить `.leave-types-row`/`.leave-types-header`/`.leave-types-*` классы (на них завязаны global правила + тесты).
- **F. Danger zone** — `<section class="gs-danger-surface surface">`: сдержанная, но однозначно опасная (НЕ красная поверхность); warnings + backup link + `data-target="#remove_company_modal"` button.
- **Modals** — `{{> add_new_leave_type_modal ...}}` + `{{> remove_company_modal ...}}` (дословно).
- **Нет card-in-card:** одна raised `.surface` на смысловом уровне; формы внутри без собственного card-хрома.
- **Нет legacy `.panel`/`.panel-default`/`.panel-danger`** в основном шаблоне (unit-контракт это проверяет).

## 2. Scoped SCSS (EOF-append; чистый append под `.general-settings-page`)
- `.general-settings-page` scoped токены (light) + `[data-theme="dark"] .general-settings-page` (dark) — копия Stage 8C skeleton. НЕ `:root`.
- `.surface` raised (opaque, calm border/radius/shadow, padding) — единый с 8A–8E. Neutralise legacy `.admin-form-card`/`.panel` chrome внутри `.surface` если нужно (нет card-in-card).
- `.page-heading h1`/`.lead` scoped.
- 6 смысловых surfaces: `.gs-company-surface`, `.gs-schedule-surface`, `.gs-carryover-surface`, `.gs-leavetypes-surface`, `.gs-related-surface`, `.gs-danger-surface` — каждая с section heading (calm, muted, ≥12px px).
- **Leave types editor:** desktop — ясная выровненная сетка (`.leave-types-header` + `.leave-types-row` columns); mobile (≤768px) — каждая leave type → deliberate card (`.leave-types-row` становится `display:block` с scoped card-хромом; name главный; allowance/auto-approve/limit/deduction/min — видимые контекстные подписи; color picker + remove — самостоятельные controls ≥44px). НЕ `overflow:hidden` на карточках.
- **Forms:** label/input/help рядом; help muted (≠ основной текст); checkbox 44px label hit-area; Save primary; carry-over визуально отличить; Add+Save сгруппированы но различимы.
- Press-feedback: `:active` + `:hover:active { transform: scale(.98) }` ~100ms (compound выигрывает над hover). Hover только `@media(hover:hover) and (pointer:fine)`.
- `:focus-visible` outline 2px strong.
- Tap targets ≥44px: Save/Add/Remove/color/backup/bank-holidays/danger (mobile).
- `@media(prefers-contrast:more)`: толще border, без shadow.
- `@media(prefers-reduced-transparency:reduce)`: solid surface.
- `@media(prefers-reduced-motion:reduce)`: `transition/animation:none` AND `transform:none` на `:active` AND `:hover:active` (каскад-урок 8B/8C/8D/8E — покрыть compound).
- `@media(max-width:768px)`: один столбец; нет горизонтального document scroll; длинные names переносятся (`overflow-wrap:anywhere`); leave-types rows → cards; controls ≥44px; checkbox 44px label.
- Dark theme: page tokens, без светлых Bootstrap background/border leaks внутри surfaces и leave-type cards.
- px, не rem (Bootstrap html=10px). Проверять весь Stage 8F scope.
- Rebuild `public/css/style.css` + `.map`.

## 3. Тесты (TDD: failing unit FIRST)
**Unit** `t/unit/general_settings_workspace_contract.js`:
- `.general-settings-page` на main; один h1 + `.page-heading`; 6 смысловых surfaces; НЕТ legacy `.panel`/`.panel-default`/`.panel-danger` в шаблоне; все form IDs/methods/actions/CSRF; company field names; company_wide=1; все leave-type names/hooks/data-attributes; modal targets; backup+bank-holidays href; accessible names leave-type controls; scoped tokens не `:root`; нет новых global Bootstrap overrides; dark theme; mobile breakpoint; reduced-motion/reduced-transparency/prefers-contrast; reduced-motion покрывает `:hover:active` compound; typography px не rem во всём scope; compiled CSS содержит скомпилированный контракт. Анализировать полный scoped block, не первые N символов.

**Selenium** `t/integration/settings/general_settings_workspace_contract.js`:
- Desktop: все surfaces; h1 единственный; формы/controls видимы; leave-types editor сетка; Add/Save/backup/bank-holidays/danger доступны; color picker открывается/закрывается без данных; modal triggers открывают правильные modal; destructive submit НЕ выполняется.
- Keyboard: реальные Tab; пройти всю `.general-settings-page` до выхода focus; основные формы/backup/leave-type/danger достижимы; нет скрытых focus targets; visible focus ring (computed style); Escape закрывает dropdown/modal где поддерживается Bootstrap.
- Mobile 390×844: `scrollWidth <= clientWidth`; каждый surface внутри viewport; leave-types карточки; измерять все controls/текст (Range.getClientRects + border-box); Save/Add/Remove/color/backup/bank-holidays/danger ≥44×44px; длинные names переносятся; нет clipping в RU/KK.
- Geometry probe: glyph rects через Range.getClientRects; border-box дочерних; горизонтальные границы vs control/card/surface/viewport; scrollWidth/clientWidth + scrollHeight/clientHeight карточек/полей; вертикальный выход за 844 — не ошибка.
- Locales: EN/RU/KK тот же geometry probe.
- Themes/media: light/dark; WCAG AA contrast; CDP prefers-reduced-motion (matchMedia proof + computed transform none на press/hover-active); reset после.
- НЕ нажимать: carry-over submit; delete company; leave-type delete; другие необратимые действия.
- Visual matrix → `/tmp/stage8f-general-settings/`: 12 screenshots (desktop light верх + leave-types/danger; desktop dark верх + leave-types/danger; mobile light верх + leave-types; mobile dark верх + leave-types; mobile RU light/dark; mobile KK light/dark). Фактически просмотреть.

## 4. Regression
Новый unit + Selenium; `settings_accessibility_contract.js`; unit settings/company/leave-types/schedule; leave-type CRUD/colouring/auto-approve/limits/minimum/remove-used; company-wide schedule; timezone; Team View share/visibility; destructive_settings_action_safety; полный unit suite; полный npm test (если стабильно). Pre-existing flake — воспроизвести на clean base `e3811a4`, приложить команды обеих попыток.

## 5. Review + commits
Whole-branch review относительно `origin/master` (`e3811a4`): visual regressions, a11y, security/privacy, responsive geometry, cascade/specificity, protected form/JS contracts, unintended global CSS, scope creep. Проверить известные ловушки прошлых этапов (mobile rule проиграл desktop специфичности; global Bootstrap background протёк в dark; `:hover` перекрыл `:active`; `:hover:active` не нейтрализован reduced-motion; overflow-wrap не работает из-за nowrap; карточка overflow:hidden; geometry меряет только td; скрытый thead/wrapper оставил focusable control; card-in-card; невалидная HTML model).

Коммиты: `feat(ui): redesign general settings workspace` + `test(settings): add visual and interaction contracts` (+ `fix(settings): ...` только для реальных review findings). Не смешивать production и cleanup.

## 6. Запреты
Не push/PR/merge/squash/rebase/force/admin/branch-protection. Не менять backend/business-rules/real-delete/real-carry-over. Не начинать Stage 8G. Не редизайнить Bank Holidays/Work Calendars. Не добавлять deps/ИИ-черновики/screenshots в репо. После реализации+тестов+review — стоп.

## Исполнение (SDD, цикл 8A–8E)
Combined brief (failing unit → markup + scoped CSS → green → commit), затем Selenium contract + visual matrix, затем regression + whole-branch review. Все защищённые контракты сохранены дословно. `settings_general.js`, routes, locales, deps — нетронуты.